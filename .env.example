# Environment Variables Example for Local Development
# 이 파일을 복사하여 .env로 저장하고 실제 값으로 수정하세요.

# Google Gemini API Key (필수)
# https://aistudio.google.com/app/apikey 에서 발급받으세요
GOOGLE_GEMINI_API_KEY=AIzaSy... # 실제 API 키로 교체

# 기본 설정값들
DEFAULT_MIN_WAIT=3
DEFAULT_MAX_WAIT=7
DEFAULT_USE_DYNAMIC_IP=false

# 네이버 계정 정보 (선택사항 - UI에서 입력 권장)
# 보안상 여기에 저장하지 말고 앱에서 직접 입력하는 것을 권장합니다.
# NAVER_DEFAULT_ACCOUNT_ID=your-naver-id
# NAVER_DEFAULT_ACCOUNT_PASSWORD=your-naver-password

# Selenium 설정
CHROME_DRIVER_PATH=
HEADLESS_MODE=false
WINDOW_SIZE=1920,1080

# 디버그 설정
ENABLE_DEBUG_LOGS=true
LOG_SELENIUM_ACTIONS=true

# 개발 환경 설정
ENVIRONMENT=development
DEBUG=true